using UnityEngine;
using System.Collections;

public class RopeScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	/*
	Fires the rope
	- pos = the inital position from where the rope will be fired
	- angle = the angle, in degrees, from which the rope will be fired. 
		Examples:
		0 = Straight up
		90 = To the right
		-90 = To the left
	- range = the range of the fired rope, in Unity units
	*/
	
	public void Fire(Vector2 pos, float angle, float range){

	}
}
